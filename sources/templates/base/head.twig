<!doctype html>
<html>
    <head>
        <meta charset="utf-8" />

        <title>{{ globals.meta.title }}{% if subtitle %} - {{ subtitle }}{% endif %}</title>
        <meta name="viewport" content="{{ globals.meta.viewport }}" />
        <meta name="format-detection" content="telephone=no" />
        <meta name="robots" content="index, follow" />
        <meta name="description" content="{{ globals.meta.description }}" />

        <meta http-equiv="x-ua-compatible" content="ie=edge" />

        <meta name="og:title" content="{{ globals.meta.title }}{% if subtitle %} - {{ subtitle }}{% endif %}" />
        <meta name="og:description" content="{{ globals.meta.description }}" />
        <meta name="og:image" content="" />

        <link rel="shortcut icon" href="{{ globals.meta.favicon }}" />
        {# generate all from https://realfavicongenerator.net/ #}

        <!-- inject:css --><!-- endinject -->
    </head>
